{
  "name": "threats-0rce",
  "version": "0.1.0",
  "private": true,
  "description": "threats.0rce.com — CTI platform production smoke/audit + monitor altyapısı. Read-only.",
  "scripts": {
    "smoke:production": "node scripts/production-smoke.mjs",
    "audit:production": "node scripts/production-smoke.mjs --audit",
    "test:parser": "node --test test/production-smoke.test.mjs",
    "monitor:check": "node scripts/production-monitor.mjs",
    "monitor:dry-run": "node scripts/production-monitor.mjs --dry-run",
    "monitor:test": "node --test test/production-monitor.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
