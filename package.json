{
  "name": "threats-0rce",
  "version": "0.1.0",
  "private": true,
  "description": "threats.0rce.com — CTI platform production smoke/audit + monitor altyapısı. Read-only.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:smoke": "node --test test/production-smoke.test.mjs",
    "test:monitor": "node --test test/production-monitor.test.mjs",
    "test:health": "node --test test/health.test.mjs",
    "test:retry": "node --test test/http-retry.test.mjs",
    "smoke": "node scripts/production-smoke.mjs",
    "audit": "node scripts/production-smoke.mjs --audit",
    "monitor:check": "node scripts/production-monitor.mjs",
    "monitor:dry-run": "node scripts/production-monitor.mjs --dry-run",
    "monitor:test": "node --test test/production-monitor.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
