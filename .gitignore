# monitor state / lock — asla commit edilmez
.runtime/
/var/lib/threats-monitor/
*.state.lock
threats-monitor-state.json
**/threats-monitor-state.json

# secret env — asla commit edilmez
/etc/threats-monitor.env
.env

# node / logs
node_modules/
*.log

# runtime / data / build artifact — asla commit edilmez
app/.next/
app/node_modules/
backups/
db/
app-runtime/
reports/
